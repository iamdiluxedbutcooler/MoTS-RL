env_name: "MiniGrid-Empty-8x8-v0"
policy_type: "ppo"
total_timesteps: 500000
num_seeds: 20
lr: 0.0003
gamma: 0.99
lambda_kl: 0.01
window_size: 1000

alpha: [0.95, 0.95, 0.95, 0.95]
beta: [0.05, 0.05, 0.05, 0.05]

ego_mode: "mlp"
num_experts: 3
hidden_dim: 64

sensitivity_test: "alpha_095"

device: "cpu"
log_dir: "results/sensitivity/alpha_095"
use_wandb: false
